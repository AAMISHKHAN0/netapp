services:
  evolution-api:
    image: atendai/evolution-api:v2.1.1
    container_name: smartisp-evolution-api
    ports:
      - "8080:8080"
    environment:
      - SERVER_URL=http://localhost:8080
      - API_KEY=smartisp_evolution_secret_key
      - DATABASE_ENABLED=false
    restart: always
